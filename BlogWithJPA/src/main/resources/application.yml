server:
  port: 443
  ssl:
    enabled: true
    key-store: src/main/resources/bootsecurity.p12
    key-store-password: 1q2w3e4r
    key-store-type: PKCS12
    key-alias: bootsecurity

spring:
  profiles:
    active: auth
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
      format_sql: true
  jwt:
    secret: govlepel@$&
  messages:
    basename: i18n/exception
    encoding: UTF-8
  url:
    base: https://i3a606.p.ssafy.io
    #base: http://localhost:8090

  logging:
    level: info

mybatis:
  config-location: classpath:config/mybatis-config.xml