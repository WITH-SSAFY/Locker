server:
  port: 8080

spring:
  profiles:
    active: auth
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
      format_sql: true
  jwt:
    secret: govlepel@$&
  messages:
    basename: i18n/exception
    encoding: UTF-8
  url:
    #base: http://i3a606.p.ssafy.io:8080
    base: http://localhost:8080
  logging:
    level: debug

mybatis:
  config-location: classpath:config/mybatis-config.xml


