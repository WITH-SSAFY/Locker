<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="post">
    <select id="selectAllByWriter" resultType="Post">
        select p.title, p.content, p.created_at, p.updated_at, p.pid
        from post p, user u
        where p.writer=u.msrl
        and u.nickname=#{writer}
    </select>

    <insert id="insert" parameterType="Post">
        insert into post(title, writer, content, created_at, updated_at)
        values(#{title}, #{writer}, #{content}, now(), now())
    </insert>

    <select id="selectAll" resultType="Post">
        select *
        from post
    </select>

    <select id="selectOne" parameterType="String" resultType="Post">
        select *
        from post
        where pid=#{pid}
    </select>

    <update id="update" parameterType="Post">
        update post
        set title=#{title}, content=#{content}, updated_at=now()
        where pid=#{pid}
    </update>

    <delete id="delete" parameterType="String">
        delete from post where pid=#{pid}
    </delete>
</mapper>